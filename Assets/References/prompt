之后的语言请使用中文回答。
我简单介绍一下我们正在进行的项目：HelioBrush。
我们的项目分为若干个界面。具体来讲它们分为两类界面，具体来讲是四个界面：第一类负责笔刷的逻辑如何画，已有的实现是HelioBrush和AdaptiBrush；第二类负责其它的东西，比如笔刷如何定义（BrushEditorController），无动作模式（NoActionController）等。
具体来讲：对于第一类，我们抽出了笔刷的行为放到了BrushBase类里边成为各自的BrushController，然后AdaptiBrush和HelioBrush继承自它。BaseBrushController和IBrushController定义了一些基础的东西（但是你可能需要修改合并这两个类。经过了太长时间了，我几乎忘记这两个类的功能了。）另外，它们具体的算法支持则体现为各自的Algorithm类。在Brush当中，我们也可以仿照AdaptiBrush和HelioBrush来实现其它的笔刷。对于第二种，我们有一个BrushEditorController类，它负责处理用户在编辑笔刷时的交互逻辑。它允许用户通过VR控制器来创建和编辑笔刷形状，并且可以保存为json文件。我们也有一个NoActionController类，它负责处理无动作模式下的交互逻辑。在这个模式下，用户可以通过VR控制器来移动视角，而不会影响笔刷的绘制。
另外，对于视觉元素我们做了一些复用。具体来讲，我们由组合的元素和原子元素，以及面向对象下的模型。原子元素是一些基本的形状，而组合的元素不仅组合了原子形状，还引用了Model并有自己的方法来构建交互的逻辑。
首先我希望你：
1.修正AdaptiBrush的逻辑。目前它的逻辑似乎是不正常的。我们不打算复现AdaptiBrush的视觉效果，因此它必须使用我们的视觉元素，但是我们打算复现AdaptiBrush笔刷控制逻辑：在笔迹被逐帧生成的过程中，控制器的位置和方向和历史笔迹究竟如何决定当前帧生成的笔记位置和方向。对于具体的方法请参考Assets\References\AdaptiBrush.md的描述。注意，请保持视觉效果一致。
2.按照我们的架构图来检查我们是否还有不太符合架构图的地方。


1.我们复现了AdaptiBrush，并且已经初步实现了一个自己的HelioBrush算法。
2.AdaptiBrush的算法在Assets\References\AdaptiBrush.md当中有介绍。604388bb8da145a736b54027f206ba4d7076b0d0的git提交成功实现了这种笔刷。
3.HelioBrush的核心算法是这样：我们使用控制器的位置来控制笔刷的位置，但是对于笔刷的方向，我们采用如下方法控制。注意，HelioBrush目前在程序上的实现上使用了丝带状笔刷，但是它不仅仅应用于丝带状笔刷，而是应用于多种平面形状甚至自定义形状的笔刷。
a.当HelioBrush笔刷绘制还未开始或者刚刚开始的时候，笔刷方向与预览的控制器方向保持一致。
b.当HelioBrush笔刷绘制正在进行的时候，我们将笔刷移动类比于飞机航行，这样就会有Pitch, Yaw, Roll这三个局部的方向。如果你能参考图示，那么图示在这里c:\Users\ADMINI~1\AppData\Local\Temp\rotations.jpg。
对于b，我们的算法是：
b1.Yaw方向和Pitch方向变化按照过去笔刷形成的轨迹推算当前移动的曲率来决定（这两个方向说白了也就是类似于开车过程中的左右转然后加个上下转），这两者和控制器的旋转姿态无关，仅仅和位置有关。（这好像和AdaptiBrush是类似的？但我不确定AdaptiBrush是怎么控制的，它好像是用了一个复杂的优化目标，但我们不用优化目标，我们做的是直接控制）每一帧，这个值都会被重新计算，也就是说它和上一次的计算结果无关。
b2.Roll方向的控制会非常复杂，并且这个值会和最近一帧和当前帧的控制器的旋转姿态有关（当然最好的是用最近几帧来做平滑）。我们并非像AdaptiBrush一样使用某个优化目标来间接决定，而是这样：我们用一个值来控制Roll的“变化率”（它的值将是rad/m，体现在空间上，我需要强调它不是rad/s或者别的什么东西，它和时间无关，也就是它在长度上的变化率）。这个值是在笔画绘制的过程中“累积”的。当笔刷开始绘制时候，这个值会被定义（创建）并且会被设定成0；然后对于绘制过程中，当前帧和上一帧我们会有控制器的姿态（旋转）信息，我们利用两个姿态的差来算出这两个姿态过渡的旋转轴和旋转的角度（应该使用四元数旋转来推算）；然后我们计算旋转轴与当前笔画移动方向所成的夹角，cos一下这个夹角，然后把旋转的角度乘上cos这个数给到给到这个累积的值上（注意，有正负数的，因此twisting，也就是roll的方向会有正反方向）。
b2注：我们给出了一些其它琐碎的的机制。但总的来讲，Roll的机制如b2所示，这些琐碎的机制对它的表现应当不大。
4.我们实现了拓展的HelioBrush叫做HelioBrushExtended，后来我把它合并回了HelioBrush形成新的HelioBrush。它的算法和HelioBrush基本一致，但是它允许用户自定义笔刷形状（而不是只能是丝带状笔刷）。用户可以通过json文件来定义笔刷形状，程序会读取这些json文件然后把它们的名字动态加载到UI的下拉列表里边让用户选择。json文件中存储的笔刷形状信息是通用的，我们已经给出了三角形，四边形，椭圆等多种形状（当然椭圆其实是致密的多边形）。目前我们已经实现了这个功能，并且创建了一些预定义的json文件放在Assets\Brushes里边。
5.我们基本实现了自定义helioBrush功能的一个演示版本。这项功能旨在让用户在为HelioBrush通过VR下互动创造新的自定义笔刷。它是一个单独的模式，用户可以在这个模式下通过控制器来创造笔刷（而非使用笔刷！）。大体的模式如下：根据左手控制器的旋转创造一个极坐标平面，旋转姿态要高平滑。坐标原点就是左手控制器的位置；而右手负责在这个平面上绘制笔刷。但是目前还没有成功实现保存笔刷的功能。我们已经定义了基于极坐标表示的笔刷，并且兼容直角坐标。






现在我们有两个目标：
1.解决笔刷存储的问题。也就是BrushEditor模式下创造的笔刷要能保存为json文件，放在Assets/Brushes里边。命名方式任意，建议使用时间来命名。
2.我需要你在反馈中能够为我讲解目前我们项目的具体结构，以及所有文件的作用，我需要你帮我梳理一下。进而我们可以探讨哪些文件是必要的，哪些可以删除，哪些需要整理，如何整理，等等。






之后的语言请使用中文回答。
我简单介绍一下我们正在进行的项目：
1.我们复现了AdaptiBrush，并且试图实现一个自己的HelioBrush算法。
2.AdaptiBrush的算法在Assets\References\AdaptiBrush.md当中有介绍。604388bb8da145a736b54027f206ba4d7076b0d0的git提交成功实现了这种笔刷。
3.HelioBrush的核心算法是这样：我们使用控制器的位置来控制笔刷的位置，但是对于笔刷的方向，我们采用如下方法控制。注意，HelioBrush目前在程序上的实现上使用了丝带状笔刷，但是它不仅仅应用于丝带状笔刷，而是应用于多种平面形状甚至自定义形状的笔刷。
a.当HelioBrush笔刷绘制还未开始或者刚刚开始的时候，笔刷方向与预览的控制器方向保持一致。
b.当HelioBrush笔刷绘制正在进行的时候，我们将笔刷移动类比于飞机航行，这样就会有Pitch, Yaw, Roll这三个局部的方向。如果你能参考图示，那么图示在这里c:\Users\ADMINI~1\AppData\Local\Temp\rotations.jpg。我们的算法是：
b1.Yaw方向和Pitch方向变化按照过去笔刷形成的轨迹推算当前移动的曲率来决定（这两个方向说白了也就是类似于开车过程中的左右转然后加个上下转），这两者和控制器的旋转姿态无关，仅仅和位置有关。（这好像和AdaptiBrush是类似的？但我不确定AdaptiBrush是怎么控制的，它好像是用了一个复杂的优化目标，但我们不用优化目标，我们做的是直接控制）每一帧，这个值都会被重新计算，也就是说它和上一次的计算结果无关。
b2.Roll方向的控制会非常复杂，并且这个值会和最近一帧和当前帧的控制器的旋转姿态有关（当然最好的是用最近几帧来做平滑）。我们并非像AdaptiBrush一样使用某个优化目标来间接决定，而是这样：我们用一个值来控制Roll的“变化率”（它的值将是rad/m，体现在空间上，我需要强调它不是rad/s或者别的什么东西，它和时间无关，也就是它在长度上的变化率）。这个值是在笔画绘制的过程中“累积”的。当笔刷开始绘制时候，这个值会被定义（创建）并且会被设定成0；然后对于绘制过程中，当前帧和上一帧我们会有控制器的姿态（旋转）信息，我们利用两个姿态的差来算出这两个姿态过渡的旋转轴和旋转的角度（应该使用四元数旋转来推算）；然后我们计算旋转轴与当前笔画移动方向所成的夹角，cos一下这个夹角，然后把旋转的角度乘上cos这个数给到给到这个累积的值上（注意，有正负数的，因此twisting，也就是roll的方向会有正反方向）。
b2注1：我们给出了累积的一个倍率，把它调小了一点。
b2注2：我们给出了一些其它琐碎的的机制。
4.我们实现了拓展的HelioBrush叫做HelioBrushExtended。它的算法和HelioBrush基本一致，但是它允许用户自定义笔刷形状（而不是只能是丝带状笔刷）。用户可以通过json文件来定义笔刷形状，程序会读取这些json文件然后把它们的名字动态加载到UI的下拉列表里边让用户选择。json文件中存储的笔刷形状信息是通用的，可以表示线段，三角形，四边形，椭圆等多种形状（当然椭圆其实是致密的多边形）。目前我们已经实现了这个功能，并且创建了一些预定义的json文件放在Assets\Brushes里边。

现在我们打算针对HelioBrush实现一个新的功能。这项功能旨在让用户在使用HelioBrushExtended的情况下能够自己互动创造笔刷。
首先我们已经有json来定义笔刷，但我不知道现在Json是如何定义的。它可能只是简单的定义为多边形。如果是这样，那么但是现在我们不能简单的定义为多边形了。要考虑到这个Json可能由多个笔画组成，这个笔画可能是封闭的，也可能不是封闭的。（当然，它们还会是多个线段的集合）。所以，请修改其数据结构，并且修改依照新的数据结构修改现有Json的表达，以及对读取该数据结构的处理，这可能涉及到笔刷预览和实际绘制的读取部分等等。
另外是用户创作的交互模式。原来我们有一个Brush Type的Dropdown（应该可以参见BrushUIManger）。现在我们考虑更正它的作用，暂时为了方便实现，我们也更正它称作行为模式种类。目前它里边放了三种笔刷，现在我们将HelioBrush和HelioBrushExtended合并（因为现在装填的HelioBrush可以看作HelioBrushExtended的一个特例，它是由极坐标上x轴正负轴上的两个顶点），所以现在我们有两种行为模式：AdaptiBrush和HelioBrush，这是因为之后把HelioBrushExtended合并和替换原HelioBrush的功能后会更名为HelioBrush。然后再有额外的两个行为模式。
第一个模式是，没有笔刷，纯视野移动；
第二个模式就是为HelioBrushExtended增加一个“编辑笔刷”的模式。用户可以在这个模式下通过控制器来创造笔刷（而非使用笔刷！）。大体的模式如下：根据左手控制器的旋转创造一个极坐标平面，旋转姿态要高平滑。坐标原点就是左手控制器的位置；而右手负责在这个平面上绘制笔刷。
在具体到笔刷绘制的方法来讲，我们会有两种状态和三种操作：状态我们有：a.绘制状态。这时候会记录上一个点的信息，接下来绘制的点会连接到这个点。 b.非绘制状态。这时候新绘制的点不会连接到任何点。这两种状态不用可以存储，只要看上一个点是不是null就可以。操作我们有：a.像是Oculus（但我用的OpenXR的API）的a键，你可以单击（不能长按）就绘制一个点。绘制状态下将新绘制的点连接“上一个点”（这里是引用而非值，同下）并替换之为新的“上一个点”，非绘制状态下则没有连接的过程；b. 按下扳机取消和上一个点的链接，非绘制状态下无效，绘制状态下将“上一个点”替换为空；然后检查上一个点是否是孤立点，如果是孤立点则删除之。c. 长按a键后高亮离控制器最近的点，松开时将当前“上一个点”连接到该点（如果处于绘制状态，非绘制状态不连接），并将该点替换为“上一个点”。对于c，在绘制状态下要额外检查：1.是否重边（重边不连接）；2.是否“上一个点”就是我们这个最新的点（操作无效）。3.是否在平面上没找到这样的点（操作无效）建议在平面距离上给个检索阈值。这样用户就可以用这几个操作创造笔刷了。显然，我们的方法兼容连续多个开放Stroke的，也兼容一次甚至多次闭合的Stroke。
对于三种操作之外还有一个额外的操作，就是保存和覆盖。我们UI会给出一个按键和列表，原本在HelioBrush模式下下拉列表会显示所有Assets/Brushes下的笔刷，但是在编辑模式下我们需要多显示一项，就是新笔刷，并且默认选择到它！只有在非绘制状态下你可以按这个按键保存笔刷（当然用ray interact，交互已经实现你不用管，你绑定UI就行）。如果下拉列表选择的是新笔刷，则自动生成命名；如果是已有笔刷，则覆盖之。保存时会把笔刷保存为json文件，放在Assets/Brushes里边。命名方式任意，建议使用时间来命名。
在具体到笔刷绘制的数据结构：使用图论相关的数据结构存储。有很多数据结构可以选择，我推荐把点编上号，然后再存储边。当然如果你觉得这个不好可以给出其他方案。
在具体到坐标系的显示来讲：我们说了左手控制器会“拿着”一个很大的平面极坐标系。对于当前投影的点和线，都用球（小半径）和胶囊体（更小半径）来表示（想想分子的三维图形！），你只需要画两条坐标线于该平面：坐标线是一个是长度所在的坐标线（圆），另一条是角度坐标（射线）。然后我们对这个平面做一个透明度Mask处理。右手投影点的一定半径范围内（比如十公分）的平面采用完全不透明的显示方式；然后再往外几厘米的透明度逐渐增加，当透明度增加到极大之后完全剪切显示。
具体到坐标系的位置操作来讲：平滑。方案自选，但是建议参考一些使用RayInteract的应用是怎么做平滑的。
实现这些功能吧，有什么问题或者不确定的地方可以问。




我们现在打算彻底更换HelioBrush的逻辑如下：
当HelioBrush笔刷绘制开始时：得到初始控制器方向（3DOF）赋值为参考方向，坐标为世界坐标
当笔刷正在绘制时：将参考方向空间变换到圆盘所在的局部坐标系，将当前控制器方向也变换到圆盘所在的局部坐标系。记录他们俩在圆盘平面上旋转方向的差异，然后将当前控制器方向在局部坐标系下的其它两个旋转方向的分量覆盖给参考方向在局部坐标系下的其它两个旋转方向的分量，将一个未修改的分量和两个已经修改的分量重新组成局部坐标系下新的旋转坐标，再转换为世界坐标作为新的参考方向。
这个差异便是用于控制螺旋twisting的“程度”（当然这个量是有正负的），单位为rad/meter。当这个差异大时候，twisting的程度也跟着变大；否则差异小的时候，这个程度也会变小。1
有几点我想告诉你，记得检查：
1.在HelioBrush里边，当你按下控制键之后，笔刷的xyz的位置虽然和控制器一致，但是旋转就和控制器没有直接关系了。
2.我看了pitch，roll，yaw的定义如图所示c:\Users\ADMINI~1\AppData\Local\Temp\rotations.jpg，这个图是飞机的图，如果我们把我们的笔刷移动看作飞机飞行的话的话，我们的pitch是不变的（当然如果说我们拓展笔刷，不再是丝带状笔刷，那么pitch就有意义了，它会和Yaw一样受到位置控制），然后Yaw是由位置变化定义的（可能是看曲率？），最后Roll则是由我们的控制器通过控制Roll的“速度（rad/m）”来间接决定。对于Roll，在笔刷开始的时候，位置是由controller决定，但是在绘制过程中就是间接决定了。
另外我想说AdaptiBrush的实现目前是有问题的，请你参考604388bb8da145a736b54027f206ba4d7076b0d0的git提交看看他们的AdaptiBrush是怎么实现的。注意哈，重新实现的过程中你需要保留我们笔刷的预览（黄色圆盘+蓝色圆柱）
现在请你检查整个程序并按我要求的去修改。不懂得可以问我，但是你需要把话说得通俗明白一点。

目前已经实现的效果并不如意，甚至是错误的。但具体怎么样我也说不上来。Yaw的控制方式是没有问题的；但程序目前好像用位置来间接的控制了twisting（也就是之前飞机那个图的Roll），这是错误的；位置所控制的应该是yaw和Pitch（由于笔刷的对称性，目前对于丝带状笔刷Pitch可以不实现，但我建议你把它做了）而不是twisting（roll）。Roll应当由我们的控制器的角度来间接决定的。另外对于我请你参考604388bb8da145a736b54027f206ba4d7076b0d0的git提交，鉴于你是agent模式，你应当有权限直接访问那次提交。你可以尝试使用交互的命令行来读取哪些内容，我会给你权限。

现在我们的项目有几点问题，我希望你做出修改：
1.对于AdaptiBrush算法的复现。经过后续Agent的修改后，目前AdaptiBrush的实现反而偏离了原文中的方法，其表现尤其是在旋转上的表现与AdaptiBrush论文Assets\References\AdaptiBrush.md中仍然不符。请确认604388bb8da145a736b54027f206ba4d7076b0d0的git提交。确保其绘制的逻辑和其一致（现在不得已拆分成了很多部分，但你需要保证其等价）你现在是agent模式，你可以用命令来确认该次提交的内容；你应该也有其它的方法，所以无需向我申请权限。现在涉及到论文中实现的方法是Assets\Scripts\Brush\AdaptiBrushController.cs和Assets\Scripts\Core\AdaptiBrushAlgorithm.cs。必要时你也可以查看其他文件。
2.我们计划对HelioBrush增加了一个“缓冲值”的机制：“假设原本的累计值为a，我们新定义一个缓冲阈值为h，现在我们使用原来计算a的方式计算atemp，然后先判断atemp和a的差是否超过了这个缓冲阈值，如果超过这个阈值将a拉向atemp直到atemp进入a的绳子h距离内（可以理解为a和atemp两个值之间有一根弹性绳子，atemp拖拽着a在变化，a是实际的值）”
3.HelioBrush算法在某些情况下还是很难控制，针对b2我看到了agent给我实现了太多机制，我理解起来有些费劲。我不希望又太多机制，因为太多机制会导致不可控。
我在b2中提到，“对于绘制过程中，当前帧和上一帧我们会有控制器的姿态（旋转）信息，我们利用两个姿态的差来算出这两个姿态过渡的旋转轴和旋转的角度（应该使用四元数旋转来推算）”我想知道如果我在远处画一个小圆会不会导致这个角度累积的很大（这就类似于我们轻轻的用仙女棒画了一个圈，按道理说找出旋转轴然后看旋转角度似乎会很大，似乎求出来累积的角度也会很大），但我不熟悉四元数，因此我不知道我说的是不是有错。如果我的猜想是对的，那么我们需要根据姿态的差来给累积的值加权，或者在一些较小的值干脆不给加权；如果我的猜想是错的，那么我觉得可以直接给求出来将要累积的值去加一个阈值，只有超过阈值才会被累积。我记得之前的agent给我在代码中做出了一些解决，但是我记不清了，能否帮我找出代码并且总结一下？
4.对于HelioBrush，现在已经很好地一个点是，3当中提出的小姿态变化导致大旋转的问题得到了缓解，我希望对于小姿态的判断再松一点，有时候画一些不需要twist的结构还是会变twist，但是大姿态变化导致大旋转所导致的增加rad/m的程度实在是过于剧烈了，我不知道是不是额外加了权重之类的，这不好，我现在都把roll sensitivity调成0.01了，之前都是0.12的。把这个改了，（然后如果你在累积缓冲值的时候，在达到一定值之后乘一个对数分之一缩小一下，这个你实现之后先注释掉，我先看第一步改完的效果，不行我把注释取消）。

现在我们准备实现一个新的功能，算法基本上和HelioBrush一致（甚至可以用相同的algorithm类），但是Component用的类要叫做HelioBrushExtended。我们要给出其它形状的笔刷（HelioBrush的笔刷是线段，而在Extended功能里边我们可以考虑做多边形笔刷，除了线段，考虑三角形和四边形，其它形状也可以考虑椭圆等等（致密的多边形）但是注意，这个三角形和四边形要用一种通用的方式去存储，如使用json存储线段信息，线段信息可以考虑用极坐标（当然平面直角坐标也行，但我感觉极坐标好些），这样我们可以方便拓展其它的笔刷，基于这种表示用户才能自定义笔刷）。
我给了你一个新的UI，你可以在下拉列表里边读。然后存储的话我还没考虑好，但是笔刷形状一定要和程序解耦，就是说不能写在程序里而是由文件（如json文件）定义。）
你需要实现背后的程序，顺便给我创建几个预定义的json表示笔刷形状。你耶可以先考虑放在哪里合适，我觉得可以放在Assets\Brushes里边。我已经在UI里边做了一个下拉列表，你可以在inspector当中让我直接把component弄个字段绑定到这个下拉列表，然后component读你需要在读过json列表后把笔刷名字动态加载到下拉列表里边，让用户自己选择。
请实现这个新功能。




之后的语言请使用中文回答。
我简单介绍一下我们正在进行的项目：
1.我们复现了AdaptiBrush，并且试图实现一个自己的HelioBrush算法。
2.AdaptiBrush的算法在Assets\References\AdaptiBrush.md当中有介绍。604388bb8da145a736b54027f206ba4d7076b0d0的git提交成功实现了这种笔刷。
3.HelioBrush的核心算法是这样：我们使用控制器的位置来控制笔刷的位置，但是对于笔刷的方向，我们采用如下方法控制。注意，HelioBrush目前在程序上的实现上使用了丝带状笔刷，但是它不仅仅应用于丝带状笔刷，而是应用于多种平面形状甚至自定义形状的笔刷。
a.当HelioBrush笔刷绘制还未开始或者刚刚开始的时候，笔刷方向与预览的控制器方向保持一致。
b.当HelioBrush笔刷绘制正在进行的时候，我们将笔刷移动类比于飞机航行，这样就会有Pitch, Yaw, Roll这三个局部的方向。如果你能参考图示，那么图示在这里c:\Users\ADMINI~1\AppData\Local\Temp\rotations.jpg。我们的算法是：
b1.Yaw方向和Pitch方向变化按照过去笔刷形成的轨迹推算当前移动的曲率来决定（这两个方向说白了也就是类似于开车过程中的左右转然后加个上下转），这两者和控制器的旋转姿态无关，仅仅和位置有关。（这好像和AdaptiBrush是类似的？但我不确定AdaptiBrush是怎么控制的，它好像是用了一个复杂的优化目标，但我们不用优化目标，我们做的是直接控制）每一帧，这个值都会被重新计算，也就是说它和上一次的计算结果无关。
b2.Roll方向的控制会非常复杂，并且这个值会和最近一帧和当前帧的控制器的旋转姿态有关（当然最好的是用最近几帧来做平滑）。我们并非像AdaptiBrush一样使用某个优化目标来间接决定，而是这样：我们用一个值来控制Roll的“变化率”（它的值将是rad/m，体现在空间上，我需要强调它不是rad/s或者别的什么东西，它和时间无关，也就是它在长度上的变化率）。这个值是在笔画绘制的过程中“累积”的。当笔刷开始绘制时候，这个值会被定义（创建）并且会被设定成0；然后对于绘制过程中，当前帧和上一帧我们会有控制器的姿态（旋转）信息，我们利用两个姿态的差来算出这两个姿态过渡的旋转轴和旋转的角度（应该使用四元数旋转来推算）；然后我们计算旋转轴与当前笔画移动方向所成的夹角，cos一下这个夹角，然后把旋转的角度乘上cos这个数给到给到这个累积的值上（注意，有正负数的，因此twisting，也就是roll的方向会有正反方向）。
b2注1：我们给出了累积的一个倍率，把它调小了一点。
b2注2：我们给出了一些其它琐碎的的机制。
4.我们实现了拓展的HelioBrush叫做HelioBrushExtended。它的算法和HelioBrush基本一致，但是它允许用户自定义笔刷形状（而不是只能是丝带状笔刷）。用户可以通过json文件来定义笔刷形状，程序会读取这些json文件然后把它们的名字动态加载到UI的下拉列表里边让用户选择。json文件中存储的笔刷形状信息是通用的，可以表示线段，三角形，四边形，椭圆等多种形状（当然椭圆其实是致密的多边形）。目前我们已经实现了这个功能，并且创建了一些预定义的json文件放在Assets\Brushes里边。

我们本打算解决如下问题，这些问题几乎全部指向笔刷的预览：
1. 对于笔刷预览，我最早在复现AdaptiBrush和创造HelioBrush的时候，笔刷预览是一个黄色的圆盘和一个蓝色的圆柱体（圆柱体表示笔刷的方向）。我设计这个蓝色圆柱体的目的是为了直观地展示笔刷的方向和朝向，而这个笔刷的轨迹是丝带状的，那么就意味着笔刷可以理解为一个线段，也就是你拖动着这个线段在画东西。但是在HelioBrushExtended中，我们需要考虑如何将这种预览扩展到多边形和其他形状。黄色圆盘或许还有意义，但是蓝色圆柱体已经没有任何意义。
这个问题深入来讲，就是对于笔刷预览，你应当考虑尽可能地统一的数据结构表示，包括AdaptiBrush，HelioBrush和HelioBrushExtended（当然AdaptiBrush和HelioBrush的效果不能变，只不过它们是用一种“通用笔刷”；而具体用哪一种通用笔刷，那就是“线段”笔刷，画出来的就是丝带状）。现在我们的HelioBrushExtended存在两种预览，一种是棒状的，一种是取决于我们形状的，这两种预览方式需要在实现上保持一致，也就是说它们俩需要统一。
2.对于笔刷预览的视觉问题。目前实现的笔刷预览存在严重的视觉问题。具体表现为：a.对于预览笔刷的形状，没有处在绘制过程中时，应当允许用户通过摇杆或者触碰板来调整笔刷相对于控制器的朝向，就是vr手柄上的触碰板，但至少我没有看到它因摇杆变化；默认情况下，圆盘的方向（如果显示的话）应当是垂直于控制器的尖端所指向的方向（一般情况下也就是ray的方向，如果ray的方向没有向下偏移的话），然后控制器的摇杆或者触碰板的作用就是向左or向右or向上or向下旋转。这样吧，你再未绘制的过程中也把黄色圆盘展示出来，对于AdaptiBrush和helioBrush，还有它的拓展，但是圆盘要薄一些，用半透明形状。b.在绘制过程中，笔刷的预览虽然还要和控制器的位置保持实时对应，但是其旋转则是受到笔迹影响的，因此需要和当前笔迹绘制的位姿保持吻合，比如说我现在的画笔是椭圆形，然后我现在的笔迹的切面是一个横着的椭圆形，那么预览的笔刷也应当是横着的椭圆形，而不是竖着的椭圆形（当然如果笔迹的切面是竖着的椭圆形的话，预览的笔刷也应当是竖着的），也就是说，笔刷预览要和当前笔迹增加的切面完美吻合。c.笔刷抬起时，笔刷要恢复成a里边描述的状态，也就是说位置对应控制器，旋转也对应与控制器并且受到摇杆影响。d.在切换到HelioBrushExtended时，预览的笔刷要变成对应的形状（当然这个形状是从json文件里边读出来的），你得刷新一下，不然看不到，目前是得切换一下笔刷形状才能看到。
3.对于笔刷的交互问题。目前用户无法通过触碰板或摇杆来调整笔刷的朝向。这和2a是一体两面的问题。前边强调预览，而这里强调绘制的逻辑。你不仅要有正确的预览，其绘制逻辑也要表现正确。你可以参考前面两个笔刷的圆柱体-线段是怎么对应的。

这些问题被交给了其它的ai来实现，但他们实现的结果是有问题的。
1.在所有的笔刷未绘制时候展示的圆盘方向都是错误的
2.HelioBrushExtended的多边形笔刷没有正确的预览。不应该用单个的蓝色圆柱体！那个是专门用于线段笔刷（即AdaptiBrush和HelioBrush的预览方式！而HelioBrushExtended是复杂的多边形！请务必参考上一次Git提交当中是如何对多边形笔刷进行预览的。）
3.绘制的时候出现了两个圆盘！跟随控制器的那个应当隐藏！你可以认为有两种逻辑的笔刷，它们交替作用：一种在还未绘制时，一种是在绘制的时候的笔刷；但在用户看来，这是同一个笔刷！
4.由于预览不正确，我无法验证使用摇杆或者触碰板来操控笔刷方向是否正确实现！